import { useFrame } from '@react-three/fiber';
import { useWorldStore } from '../store';
import { Vector3 } from 'three';

export function PhysicsEngine() {
  // Item merging is strictly disabled per project requirements.
  // Each dropped item entity remains completely independent throughout its lifecycle.
  return null;
}

